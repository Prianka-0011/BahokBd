@model BahokBdDelivery.ViewModels.MarchentChargeVm
@{
    ViewData["Title"] = "EditCharge";
    Layout = null;
}




<div class="card mb-4">
    <div class="card-header ">
        <h2>Edit Marchent charge</h2>
    </div>
    <div class="card-body">
        <table class="table-responsive table table-striped" id="AreaList">
            <tr>
                <th>
                    <button class="btn btn-success" onclick='selectAll()'><i class="far fa-check-square"></i></button>
                    <button class="btn btn-danger" onclick='unSelectAll()'><i class="fas fa-times"></i></button>

                </th>
                <th>Area</th>
                <th>Base Charge</th>
                <th>Increase Charge/kg</th>
            </tr>
            @foreach (var area in ViewBag.editMarchentCharge)
            {
                <tr>

                    <td> <input id="AreaId" type="checkbox" value="@area.Id" class="AreaId1" name="AreaCheck" checked="@area.Status" /></td>
                    <td>
                        <label>@area.Location</label>
                        <input type="hidden" id="areaName" value="@area.Location" />
                    </td>
                    <td>
                        <input id="base" class="base form-control" value="@area.BaseCharge" />

                        <input type="hidden" id="marchentId" value="@area.MarchentId" />
                    </td>

                    <td>
                        <input id="inc" class="inc form-control" value="@area.IncreaseCharge" />
                    </td>


                </tr>
            }
            <tr>
                <td> <button type="button" onclick='editButton()' class="btn btn-info mt-3">Update Charge</button></td>
            </tr>
        </table>
    </div>
</div>



