

@{
    ViewData["Title"] = "Index";
    //  int i = 1;
}
<div class="card mb-4">
        <div class="card-header ">
            <h2>Manage Branch</h2>
            <a onclick="showInPopup('@Url.Action("CreateRole","Account",null,Context.Request.Scheme)','New Role' )" class="btn  text-black-50"><i class="fas fa-random"></i> New Role</a>
            <a onclick="showInPopup('@Url.Action("AssignRole","Account",null,Context.Request.Scheme)','Assign Role' )" class="btn  text-black-50"><i class="fas fa-random"></i> Assign Role</a>
        </div>
        <div id="view-all" class="card-body">
            @*@await Html.PartialAsync("_ViewAllBranch", Model)*@
        </div>
    </div>
